name: Build Nifty APK

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install Libraries
        run: |
          python -m pip install --upgrade pip
          python -m pip install "flet==0.28.3" requests

      - name: Verify Flet Version
        run: |
          python -c "import flet; print('Flet Python:', flet.__version__)"
          flet --version

      - name: Clean Previous Build
        run: |
          rm -rf build
          rm -rf release
          mkdir -p release

      - name: Build APK
        run: |
          yes y | flet build apk

      - name: Find APK
        run: |
          echo "Searching for generated APK..."
          find . -type f -name "*.apk" -print

      - name: Prepare Final APK
        run: |
          APK=$(find . -type f -name "*.apk" | head -n 1)

          if [ -z "$APK" ]; then
            echo "ERROR: APK was not generated."
            exit 1
          fi

          echo "Found APK: $APK"
          cp "$APK" release/NiftyPro.apk

      - name: Upload Final APK
        uses: actions/upload-artifact@v4
        with:
          name: NiftyPro-Mobile-App
          path: release/NiftyPro.apk
          if-no-files-found: error
